extends layout

block content
  div.main
    each val, index in project
      div(style= "background-image:url(" + source[index].src   + "), linear-gradient(150deg, rgba(200,200,200,0.3), rgba(0,0,0,0) 50%)").project
        a(href='/project/' + path[index].src )
          div.textProject
            h2 #{projectName}
            p #{projectDescript}
        
